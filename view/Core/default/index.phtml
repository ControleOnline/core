<?php

if (isset($response['form']) && $response['form']) {
    $form = $response['form'];
    include(dirname(__FILE__) . '/form.phtml');
} else {
    echo '<pre>';
    print_r($response);
    echo '</pre>';
} 